import{e}from"./app.8e2ec6d6.js";const n={},s=e(`<h1 id="input-and-output" tabindex="-1"><a class="header-anchor" href="#input-and-output" aria-hidden="true">#</a> Input and Output</h1><h2 id="output" tabindex="-1"><a class="header-anchor" href="#output" aria-hidden="true">#</a> Output</h2><p>There are three ways to output to the console in Java, namely:</p><ul><li><code>print()</code> - This prints text to the console.</li><li><code>println()</code> - This prints text to the console, and creates a new line.</li><li><code>printf()</code> - This allows you to print your formatted strings to the console, using various formatting specifiers.</li></ul><p>There are also escape sequences you can use in your strings, namely:</p><ul><li><code>\\b</code> - Backspace, moves your cursor one space to the left.</li><li><code>\\t</code> - Tab, moves the cursor to the next tab stop.</li><li><code>\\n</code> - Newline, moves the cursor to the beginning of the next line.</li><li><code>\\r</code> - Carriage return, moves the cursor to the beginning of the current line.</li><li><code>\\&quot;</code> - Double quotation mark, displays a double quotation mark.</li><li><code>\\&#39;</code> - Single quotation mark, displays a single quotation mark.</li><li><code>\\\\</code> - Backslash, displays a backslash.</li></ul><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>Ever wanted to know why displaying a double quotation mark, single quotation mark or backslash are in an escape sequence? This is because all three of these symbols are used in Java as identifiers. The double quotation mark can represent the start or end of a <code>String</code>, the single quotation mark can represent the start or end of a <code>char</code>, and the backslash is used for an escape sequence. Without the backslash in front of them, the compiler might think that they are the start/end of a string/char or the start of an escape sequence. With these backslashes, the compiler can easily tell that these are meant to be treated as strings and not the start/end of a string/char or the start of an escape sequence.</p></div><h2 id="input" tabindex="-1"><a class="header-anchor" href="#input" aria-hidden="true">#</a> Input</h2><p>So, now that we&#39;ve talked about output, let&#39;s talk about input. To get input in Java, we use the <code>Scanner</code> class. To use this class, you need to import it from the <code>java.util</code> package like so.</p><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code><span class="token keyword">import</span> <span class="token namespace">java<span class="token punctuation">.</span>util<span class="token punctuation">.</span></span><span class="token class-name">Scanner</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><p>This needs to be placed before your class declaration, not inside, not after.</p><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>You can actually use the <code>Scanner</code> class directly without importing it, like so.</p><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code><span class="token class-name"><span class="token namespace">java<span class="token punctuation">.</span>util<span class="token punctuation">.</span></span>Scanner</span> scanner <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name"><span class="token namespace">java<span class="token punctuation">.</span>util<span class="token punctuation">.</span></span>Scanner</span><span class="token punctuation">(</span><span class="token class-name">System</span><span class="token punctuation">.</span>in<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><p>However, this is usually considered bad practice, so just keep importing it like you&#39;d normally do. This is only ever used when you have two packages with the same class name, and you want to use both those classes.</p></div><p>If you read the tip above, you&#39;d already know how to create a new scanner, but in case you didn&#39;t, here is an example.</p><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code><span class="token class-name">Scanner</span> scanner <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Scanner</span><span class="token punctuation">(</span><span class="token class-name">System</span><span class="token punctuation">.</span>in<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>Since we already imported the scanner class, we don&#39;t need to keep typing <code>java.util.Scanner</code> over and over again.</p></div><p>Now, how do we use this scanner? For our simple input and output, we have 9 methods.</p><ul><li><code>next()</code></li><li><code>nextLine()</code></li><li><code>nextBoolean()</code></li><li><code>nextByte()</code></li><li><code>nextDouble()</code></li><li><code>nextFloat()</code></li><li><code>nextLong()</code></li><li><code>nextInt()</code></li><li><code>nextShort()</code></li></ul><p>Most of these are self-explanatory, but for <code>next()</code> and <code>nextLine()</code>, <code>next()</code> will return the first word the user inputs, while <code>nextLine()</code> will return the entire line the user inputs.</p><p>Say for example,</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>Never gonna give you up!
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><p><code>next()</code> will return &quot;Never&quot;, while <code>nextLine()</code> will return &quot;Never gonna give you up!&quot;.</p><div class="custom-container warning"><p class="custom-container-title">WARNING</p><p>Do note that this example was done with the methods used seperately. Using <code>next()</code> then using <code>nextLine()</code> will return &quot;Never&quot; and &quot; gonna give you up!&quot; respectively, since the Scanner has already passed &quot;Never&quot; using the <code>next()</code> method when you are about to use <code>nextLine()</code>, returning &quot; gonna give you up!&quot; instead.</p></div><h2 id="formatting" tabindex="-1"><a class="header-anchor" href="#formatting" aria-hidden="true">#</a> Formatting</h2><p>You&#39;ve heard of <code>printf()</code> from the start of the <code>Output</code> section, but just what does it do? Basically, you can put format specifiers in your strings, and <code>printf()</code> will format the string accordingly, depending on the variables you pass to <code>printf()</code>.</p><p>For now, you will only need these five conversion characters:</p><ul><li><code>%d</code> - decimal integer (byte, short, int and long)</li><li><code>%f</code> - floating-point number (float, double)</li><li><code>%c</code> or <code>%C</code> - character (using <code>%C</code> will captialize the character)</li><li><code>%s</code> or <code>%S</code> - string (using <code>%S</code> will capitalize all letters in the string)</li><li><code>%b</code> - boolean</li></ul><p>There are also optional flags you can use to further format your messages. You have to put these flags before the conversion character, like this: <code>%,d</code>.</p><p>Here are some useful flags that you should know:</p><ul><li><code>.</code> - supported by <code>%f</code>, denotes precision. Ex. <code>0.69420</code> formatted using <code>%.2f</code> will output <code>0.69</code>.</li><li><code>,</code> - supported by <code>%f</code> and <code>%d</code>, denotes comma seperation. Ex. <code>69420</code> formatted using <code>%,d</code> will output <code>69,420</code>.</li></ul><h2 id="advanced-knowledge-scanner" tabindex="-1"><a class="header-anchor" href="#advanced-knowledge-scanner" aria-hidden="true">#</a> Advanced Knowledge: Scanner</h2><div class="custom-container warning"><p class="custom-container-title">WARNING</p><p>In this section, we are going to dive deep into how Java&#39;s Scanner class works with console input. This information can provide a deeper understanding about console input, however mastering these details is not required nor included for any of our CS activities.</p><p>If you are willing or eager to expand and enhance your understanding of how the Scanner class works with console input, feel free to read this section. However, if you don&#39;t need to or prefer not to do so, then you are free to skip to the next section. The content included in this section is intended for people who would like to know more, and will not be a requirement for succeeding sections.</p></div><p>As you may know, the Scanner class has various methods you can use for input, but just how exactly does it read input?</p><p>When you are asked for input, the program waits for this input, and once you are done, this input is sent to <code>System.in</code>, which the Scanner class then scans your input.</p><p>Now, lets say we want to know the user&#39;s name. Our code would look a little bit like this:</p><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code><span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">&quot;Hello! What is your name?&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">String</span> name <span class="token operator">=</span> input<span class="token punctuation">.</span><span class="token function">nextLine</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>We input &quot;Joe&quot; then press <code>Enter</code>, which is then sent to <code>System.in</code>.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>Joe

</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Notice that when you press the <code>Enter</code> key, it is registered as a newline in <code>System.in</code>. Now, how does the Scanner read this? Let us imagine a cursor for the Scanner class reading, indicated by <code>|</code>.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>|Joe

</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>We are reading this using <code>input.nextLine()</code>, so the Scanner would read Joe, see the newline, consume it and end reading. Now, our <code>System.in</code> should look like this.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>Joe
|
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>This is due to how <code>input.nextLine()</code> works. It will continuously scan until it sees a newline, then it will consume that newline and end reading.</p><p>Let us try asking them for their age. We will add this block of code below the code asking for the name.</p><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code><span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">&quot;How old are you?&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">int</span> age <span class="token operator">=</span> input<span class="token punctuation">.</span><span class="token function">nextInt</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Now, let&#39;s say our age is <code>15</code>. System.in should look like this.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>Joe
|15

</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>We are reading the user&#39;s age using <code>input.nextInt()</code>, so the Scanner would read <code>15</code>, see the space, and end reading. This is because unlike <code>input.nextLine()</code>, all of the mentioned input methods don&#39;t consume the newline. Instead, they consume the next &quot;word&quot;, which is any sequence of characters before a space.</p><p>Now, our <code>System.in</code> should look like this.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>Joe
15|

</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Let&#39;s try asking them for their country. We will add this block of code below the code asking for their age.</p><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code><span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">&quot;What country are you from?&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">String</span> country <span class="token operator">=</span> input<span class="token punctuation">.</span><span class="token function">nextLine</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Now, if we try running the program, you may notice that after you input your age, the code immediately skips the input for &quot;What country are you from?&quot;! Why does this happen? Well, this is also because of how the Scanner class works.</p><p>From our <code>System.in</code> above, you can see that the &quot;cursor&quot; stops after 15. If we try to get the next line, the Scanner class would consume the newline created by inputting the number 15 instead! Our <code>System.in</code> should look like this.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>Joe
15
|
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>This is because of how <code>input.nextLine()</code> works. However, since there is no input it can scan anymore, and it already sees a newline, it will consume that newline and return <code>&quot;&quot;</code> (empty string) instead.</p><p>We can prove this works if you input &quot;15 Philippines&quot; when it asks you for your age. This time, it will still skip the question, however our <code>country</code> variable would have the value of <code>&quot; Philippines&quot;</code>. This is because the Scanner reads <code> Philippines</code>, then sees the newline, consumes it and ends reading.</p><p>Now that you know how this happens, how do you fix it? Some people fix it by putting their <code>input.nextLine()</code> methods first before the other methods, while some people use <code>input.next()</code> instead, if they are expecting a single-word answer. You can also use two <code>input.nextLine()</code> methods instead: one to consume the remaining newline, one to consume the input.</p><p>The way you fix it is entirely up to you, but now at least you can possibly identify what is wrong when your input suddenly skips over!</p>`,58);function a(t,o){return s}n.render=a;export{n as default};
