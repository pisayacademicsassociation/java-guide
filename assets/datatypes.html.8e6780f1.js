import{e}from"./app.8e2ec6d6.js";const a={},n=e(`<h1 id="data-types" tabindex="-1"><a class="header-anchor" href="#data-types" aria-hidden="true">#</a> Data types</h1><p>There are two types of data types in Java, primitive types and reference types.</p><h2 id="primitive-data-types" tabindex="-1"><a class="header-anchor" href="#primitive-data-types" aria-hidden="true">#</a> Primitive data types</h2><p>Primitive types are types predefined by the language, are named using reserved keywords, and don&#39;t share states with other primitive values.</p><p>There are eight primitive data types supported by the Java Programming Language, namely:</p><ul><li><code>byte</code>: an 8-bit signed two&#39;s complement integer that has a range of -128 to 127</li><li><code>short</code>: an 8-bit signed two&#39;s complement integer that has a range of -32,768 to -32,767</li><li><code>int</code>: an 8-bit signed two&#39;s complement integer that has a range of -2\xB3\xB9 to 2\xB3\xB9 - 1</li><li><code>long</code>: an 8-bit signed two&#39;s complement integer that has a range of -2\u2076\xB3 to 2\u2076\xB3 - 1</li><li><code>float</code>: a single-precision 32-bit floating point, with an accuracy of 6 to 7 decimal digits.</li><li><code>double</code>: a double-precision 64-bit floating point, with an accuracy of 15 decimal digits.</li><li><code>boolean</code>: has only 2 possible values: <code>true</code> and <code>false</code>.</li><li><code>char</code>: a single 16-bit Unicode character.</li></ul><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>Normally, an integer literal is of type <code>int</code>, but it can be of type <code>long</code> if you suffix it with either <code>L</code> or <code>l</code>, but it is good practive to use <code>L</code> instead of <code>l</code> because it might be mistaken for a <code>1</code>.</p><p>A floating-point literal is of type <code>float</code> if you suffix it with either <code>F</code> or <code>f</code>. Otherwise, it&#39;s type is <code>double</code>, which you can optionally suffix with <code>D</code> or <code>d</code>. You can also use <code>E</code> or <code>e</code> for scientific notation.</p><p>Additionally, you can place underscores in numeric literals, however there are some limitations to this.</p><p>You cannot place underscores:</p><ul><li>At the beginning or end of a number</li><li>Adjacent to a decimal point in a floating point literal</li><li>Before an F or L suffix</li></ul><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code><span class="token keyword">int</span> myInt <span class="token operator">=</span> <span class="token number">100_000_000</span><span class="token punctuation">;</span> <span class="token comment">// Correct</span>

<span class="token keyword">double</span> myDouble <span class="token operator">=</span> <span class="token number">69_.0020</span><span class="token punctuation">;</span> <span class="token comment">// Incorrect</span>

<span class="token keyword">float</span> myFloat <span class="token operator">=</span> <span class="token number">1.000_F</span><span class="token punctuation">;</span> <span class="token comment">// Incorrect</span>

<span class="token keyword">int</span> anotherInt <span class="token operator">=</span> <span class="token number">100_000_</span> <span class="token comment">// Incorrect</span>

<span class="token keyword">double</span> anotherDouble <span class="token operator">=</span> <span class="token number">69.420_400</span><span class="token punctuation">;</span> <span class="token comment">// Correct</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div></div><h2 id="reference-data-types" tabindex="-1"><a class="header-anchor" href="#reference-data-types" aria-hidden="true">#</a> Reference data types</h2><p>These data types refer to objects. Common examples include strings, arrays, classes and etc.</p><p>Do note that:</p><ul><li>A reference variable is created using the defined constructor of the class you want to create an instance of. These are used to store and access objects. An exception to this is the <code>String</code> class, which can be created using the String literal (<code>&quot;</code>).</li><li>These variables are declared to be of a specific type that cannot be changed.</li><li>Class objects, and various types of array variables are under reference data types.</li><li>The default value of any reference variable is null.</li><li>A reference variable can be used to refer to any object of the declared type or any compatible type.</li></ul><p>For example, say we have a <code>Square</code> class.</p><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">Square</span> <span class="token punctuation">{</span>
	<span class="token keyword">int</span> size <span class="token operator">=</span> <span class="token number">5</span><span class="token punctuation">;</span>
	<span class="token keyword">int</span> area <span class="token operator">=</span> <span class="token number">25</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Then, we create a new Square using that class.</p><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code><span class="token class-name">Square</span> mySquare <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Square</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><p>To access the size of this square, we use the <code>.</code> symbol to access the <code>size</code> property of <code>mySquare</code>.</p><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code>mySquare<span class="token punctuation">.</span>size<span class="token punctuation">;</span> <span class="token comment">// Value: 5.</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><h2 id="type-casting" tabindex="-1"><a class="header-anchor" href="#type-casting" aria-hidden="true">#</a> Type Casting</h2><p>This is a Java feature that allows you to turn a value from one data type to another data type.</p><p>There are actually two types of Type Casting, Narrow and Wide.</p><p>Wide Type Casting is turning a smaller type into a larger type.</p><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code><span class="token keyword">int</span> myInt <span class="token operator">=</span> <span class="token number">9</span><span class="token punctuation">;</span>

<span class="token keyword">double</span> myDouble <span class="token operator">=</span> myInt<span class="token punctuation">;</span> <span class="token comment">// doesn&#39;t need (double) myInt;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>What&#39;s neat about this is that it is done automatically, and that you don&#39;t need to add <code>(double)</code> prefixed to <code>myInt</code> to change it into a <code>double</code>!</p><p>Narrow Type Casting is turning a larger type into a smaller type.</p><div class="language-java ext-java line-numbers-mode"><pre class="language-java"><code><span class="token keyword">double</span> myDouble <span class="token operator">=</span> <span class="token number">69.420</span><span class="token punctuation">;</span>

<span class="token keyword">int</span> myInt <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token keyword">int</span><span class="token punctuation">)</span> myDouble<span class="token punctuation">;</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Here, you need to prefix <code>(int)</code> to properly cast this <code>double</code> into an <code>int</code>. If you don&#39;t, you get a &quot;Type mismatch: cannot convert from double to int&quot; error.</p>`,26);function s(t,o){return n}a.render=s;export{a as default};
